$(document).ready(function(){
    $(".query").addClass("animated bounceIn");
    document.getElementById("search-input").focus();
    //onclick #search
    //call input handler and creates a link
        // calls the results manipulater by passing the link
    //onclick rendom search
        //opens a random wikiarticle
});